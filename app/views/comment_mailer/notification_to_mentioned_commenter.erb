You were mentioned in a comment on our article "<%= h @article.title %>"

<%= article_url(@article) << "\#comment_#{@comment.id}" %>

<%= h @comment.name %><%= " (#{h @comment.email})" unless @comment.email.blank? %> wrote:

<%= h @comment.comment %>
